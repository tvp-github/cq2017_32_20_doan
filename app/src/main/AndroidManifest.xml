<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hcmus.android.lighttour">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".CreateTourActivity"
            android:parentActivityName=".ListTourActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ListTourActivity" />
        <activity android:name=".CreateSpotPointActivity" />
        <activity android:name=".PointInformationActivity" />
        <activity
            android:name=".WriteReviewActivity"
            android:parentActivityName=".TourInformationActivity"/>
        <activity
            android:name=".ExploreActivity"
            android:parentActivityName=".ListTourActivity"/>
        <activity
            android:name=".HistoryActivity"
            android:parentActivityName=".ListTourActivity" />

        <activity
            android:name=".TourInformationActivity"
            android:parentActivityName=".ListTourActivity"/>
        <activity
            android:name=".WriteFeedbackActivity"
            android:parentActivityName=".PointInformationActivity"/>
        <activity
            android:name=".SettingsActivity"
            android:parentActivityName=".ListTourActivity"/>
        <activity
            android:name=".NotificationActivity"
            android:parentActivityName=".ListTourActivity"/>
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".VerifyOTPActivity" />
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".EditProfileActivity" />
    </application>



</manifest>